# Copy to .splitwise.env (gitignored). Sourced by run.sh / splitwise-run.sh.
# Use EITHER a personal API key...
export SPLITWISE_API_KEY=""
# ...OR OAuth client credentials (client_credentials grant):
# export SPLITWISE_CONSUMER_KEY=""
# export SPLITWISE_CONSUMER_SECRET=""
