# Dependencies
node_modules/

# Environment / secrets (sourced by run.sh / splitwise-run.sh)
.actual.env
.splitwise.env
*.env
!*.env.example

# Personal config (copy the *.example.json template and fill in your own)
splitwise-groups.json

# Generated snapshots & imported data (never commit)
owes-truth.json
owes-truth-new.json
owes-truth*.json
venmo-truth.json
events.json
*.csv

# Logs & OS cruft
*.log
.DS_Store
