# Dependencies
node_modules/
.sessions/

# Environment / secrets
.env
.env.local
*.env
!*.env.example

# Passkey credential store uses server.js's distinct atomic writer (no .last-good).
# PR-15 follow-up: reconcile this store with STATE_REGISTRY's durability model.
passkey-credentials.json
passkey-credentials.json.*.*.tmp

# Personal config (copy the *.example.json templates and fill in your own)
personal-config.json
owes-config.json
splitwise-groups.json

# Runtime data & snapshots (generated at runtime; never commit)
owes-truth.json
venmo-truth.json
events.json
reimb-links.json
reimb-suggest.json
recurring-overrides.json
goals.json
bills-paid.json
phantom-seen.json
phantom-log.json
receipts.json
rules.json
account-overrides.json
manual-assets.json
reconciliation.json
budget-settings.json
debt-planner.json
investment-holdings.json
operation-journal.json
review-state.json
transaction-sagas.json
receipts/
*.last-good
*.corrupt-*

# Atomic JSON-store temporary files: .<state>.json.<pid>.<random>.tmp
.*.json.*.*.tmp

# Personal reference scripts (not part of the app)
.reference/

# Imported statements
*.csv

# Logs & OS cruft
*.log
.DS_Store
